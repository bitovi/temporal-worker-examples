global:
 scrape_interval: 10s
scrape_configs:
 - job_name: 'temporalmetrics'
   metrics_path: /metrics
   scheme: http
   static_configs:
     # sdk metrics
     - targets:
         - 'replay-giveaway:8077'
       labels:
         group: 'sdk-metrics'

     # replay-giveaway custom metrics
     - targets:
         - 'replay-giveaway:8000'
       labels:
         group: 'workflow-metrics'
